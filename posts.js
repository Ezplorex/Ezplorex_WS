const POSTS=[
{slug:"single-interrupt-flag-miss-events",title:"Why a single interrupt flag can make you miss events",category:"Embedded Systems",date:"August 30, 2026",excerpt:"A simple flag looks convenient, but it cannot count multiple events that happen before the main loop processes them.",content:`<p>My starting assumption was simple: if an interrupt occurs, set a flag. The main loop sees the flag and processes the event.</p><h2>Where the problem appears</h2><p>Imagine the interrupt happens twice before the main loop checks the flag. The first interrupt sets it to <code>1</code>. The second interrupt also sets it to <code>1</code>. The main loop still sees only one pending event.</p><pre>ISR #1  → eventFlag = 1
ISR #2  → eventFlag = 1
Main    → sees eventFlag = 1 only once</pre><p>A boolean flag tells us that at least one event happened, but not how many events happened.</p><h2>My takeaway</h2><p>If every event matters, I should consider an event counter, queue, or another mechanism that preserves multiple occurrences.</p>`},
{slug:"what-volatile-solves",title:"What volatile actually solves — and what it does not",category:"C Programming",date:"August 30, 2026",excerpt:"Volatile helps the compiler treat a value as capable of changing unexpectedly. It does not solve every concurrency problem.",content:`<p>I learned that <code>volatile</code> is important when a variable can change outside the normal flow of the current code, such as through an ISR or hardware register.</p><h2>What it does not solve</h2><p>Volatile does not make an operation atomic. It does not count missed events. It does not automatically make shared data safe between an ISR and the main program.</p><p>My takeaway: <code>volatile</code> is about compiler visibility, not a complete synchronization strategy.</p>`},
{slug:"component-orientation-pick-place",title:"When component orientation creates pick-and-place problems",category:"PCB Design",date:"August 30, 2026",excerpt:"A practical observation: inconsistent component orientation inside a reel can create problems for automated assembly.",content:`<p>While working around a PCB manufacturing issue, I noticed that very small components in a reel did not appear consistently oriented.</p><h2>Why this matters</h2><p>The machine and vision system expect a known component orientation and packaging format. If the actual reel does not match that expectation, recognition or rotation handling can fail.</p><p>My takeaway: component sourcing is not only about electrical specifications. Packaging quality and supplier consistency also matter.</p>`}
];